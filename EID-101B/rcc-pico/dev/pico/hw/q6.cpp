#include <iostream>
#include <unistd.h>


int main(void)
{


if ((is_stationary) && (is_battery_ok) && (is_state_ready)) enable_motor(MOTOR_A); enable_motor(MOTOR_B); return true; 
return false; 




	return 0;
}
